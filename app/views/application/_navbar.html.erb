<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <a class="navbar-brand" href="#">Squawk</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if current_user %>
        <li class="nav-item active">
          <%= link_to 'Chatrooms', chatrooms_path, class: "nav-link #{'active' if request.fullpath == chatrooms_path}" %>
        </li>
      <% end %>
      <li class="nav-item">
        <%= link_to 'Sign up', sign_up_path, class: "nav-link #{'active' if request.fullpath == sign_up_path}" %>
      </li>
      <li class="nav-item">
        <%= link_to 'Sign in', sign_in_path, class: "nav-link #{'active' if request.fullpath == sign_in_path}" %>
      </li>
    </ul>

    <% if current_user %>
      <div class="navbar-text">
        Signed in as <%= current_user.first_name %> <%= current_user.last_name %> | <%= link_to "Logout", '/logout' %>
      </div>
    <% end %>
  </div>
</nav>
